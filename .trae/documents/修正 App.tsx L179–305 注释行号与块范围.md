# 问题诊断
- 构建报错来源：在 `return (` 后直接写了 JSX 注释 `{/* ... */}`，它成为与根元素并列的子节点，未被包裹导致解析错误。
- 需调整：将顶层两行 JSX 注释移入根 `<div className="page">` 内作为首个子节点；同时校正“右栏标题与工具区”的行号范围。

# 修改方案
1. 将以下两行注释移入 `<div className="page">` 内部（作为第一个子节点）：
   - `{/* 🧱 顶层容器布局（第180–184行） */}`
   - `{/* 使用 CSS Grid 控制左右栏比例并挂载容器 ref */}`
   说明：注释仍指向 180–184 的模块范围，不必与其自身所在行一致。

2. 校正行号文本：
   - `📄 右栏标题与工具区` 行号改为 `第206–230行`（当前文件中标题块结束于 230 行）。
   - 其他 JSX 注释块行号保持不变（已与当前文件对齐）。

# 实施步骤
- 移动顶层注释至 `<div className="page">` 打开标签之后。
- 更新“右栏标题与工具区”注释的行号文本。
- 不改动业务逻辑，仅改动注释文本与位置。

# 验证
- 重新构建，确认不再出现 `Unexpected token` 报错。
- 目检所有注释行号与模块范围一致，文件可正常运行。

# 交付
- 更新后的 `web/src/App.tsx`：顶层注释位置合法、所有注释行号准确、风格统一。